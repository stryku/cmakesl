include(${CMAKESL_DIR}/cmake/cmsl_cmake_utils.cmake)

cmsl_add_test(
    NAME
        indexer_smoke
    SOURCES
        indexer_test.cpp
    INCLUDE_DIRS
        ${CMAKESL_SOURCES_DIR}
        ${CMAKESL_FACADE_SOURCES_DIR}
        ${CMAKESL_TESTS_DIR}
        ${CMAKESL_DIR}
    LIBRARIES
        lexer
        ast
        sema
        errors_observer_mock
        cmsl_tools
        tests_common
)
