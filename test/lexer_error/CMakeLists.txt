cmsl_add_test(
    NAME
        lexer_error
    SOURCES
        lexer_error_test.cpp
        main.cpp
    INCLUDE_DIRS
        ${CMAKESL_SOURCES_DIR}
        ${CMAKESL_TESTS_DIR}
    LIBRARIES
        lexer
        errors_observer_mock
)
