cmsl_add_test(
    NAME
        ast
    SOURCES
        main.cpp
        parser_test.cpp
    INCLUDE_DIRS
        ${CMAKESL_SOURCES_DIR}
        ${CMAKESL_DIR}
    LIBRARIES
        ast
        lexer
        errors
        tests_common
)

cmsl_add_test(
    NAME
        ast_error
    SOURCES
        main.cpp
        parser_test.cpp
    INCLUDE_DIRS
        ${CMAKESL_SOURCES_DIR}
        ${CMAKESL_DIR}
    LIBRARIES
        ast
        lexer
        errors_observer_mock
        tests_common
)
