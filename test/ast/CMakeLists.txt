include(${CMAKESL_DIR}/cmake/cmsl_cmake_utils.cmake)

cmsl_add_test(
    NAME
        ast
    SOURCES
        parser_test.cpp
    INCLUDE_DIRS
        ${CMAKESL_SOURCES_DIR}
        ${CMAKESL_DIR}
    LIBRARIES
        ast
        lexer
        errors
        tests_common
)

cmsl_add_test(
    NAME
        ast_error
    SOURCES
        parser_error_test.cpp
    INCLUDE_DIRS
        ${CMAKESL_SOURCES_DIR}
        ${CMAKESL_DIR}
    LIBRARIES
        ast
        lexer
        errors_observer_mock
        tests_common
)
